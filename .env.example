# Local defaults are shown; managed hosts usually inject PORT themselves.
PORT=3001
HOST=0.0.0.0
LOG_DIR=server/logs

# Optional comma-separated browser origins for a separately hosted frontend.
# Same-origin browser connections are allowed automatically.
# ALLOWED_ORIGINS=https://kicesanders.com,https://www.kicesanders.com
